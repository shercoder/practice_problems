// #2290 Ferry Loader

#include <iostream>
#include <string>
#include <list>

using namespace std;

int main() {
    int t = 0;      // test cases
    cin >> t;

    int len = 0; // ferry len
    int n = 0;
    int c_len = 0;
    int total = 0;
    string bank;
    int trips = 0;

    while(t--) {
        cin >> len;
        len *= 100;
        cin >> n;

        list<int> banks[2];
        total = 0
        while(n--) {
            cin >> c_len;
            cin >> bank;

            trips++;
        }
    }

    return 0;
}