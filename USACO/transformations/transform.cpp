/*
	ID: singh.p2
	PROG: transform
	LANG: C++
 */

#include <iostream>
#include <fstream>
#include <algorithm>

using namespace std;

void nintyDegree(char** A, int N) {
	int x = 0, y = 0;
	for(int i = 0; i < N; ++i) {
		
	}
}

int main () {
	ifstream fin ("transform.in");
	ofstream fout ("transform.out");

	int N = 0;
	
	return 0;
}